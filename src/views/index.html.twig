{% extends "base.html.twig" %}

{% block content %}
<div class="container">
  <div class="row">
    
{%for product in products%}
<div class="col-sm">
      
    
    <div class="card" style="width: 18rem;">
        <div class="card-body">
            <h5 class="card-title">{{product.nom}}</h5>
            <p class="card-text">details du produit: blablablablabla....</p>
            <p class="card-text">Pour la modique somme de: {{product.prix}}</p>
        </div>
    </div>
</div>
{%endfor%}

{% endblock %}